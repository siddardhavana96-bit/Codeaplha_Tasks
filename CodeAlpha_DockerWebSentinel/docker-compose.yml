services:
  web:
    build: .
    ports: ["8088:8080"]
    read_only: true
    tmpfs: [/tmp, /var/cache/nginx]
    security_opt: [no-new-privileges:true]
    cap_drop: [ALL]
    restart: unless-stopped
